# Simplified (very) epidemic simulation

To run you need `python 3.7+`. In parent directory (to this repo) run:
```bash
python3 -m epidemic
```
Then you should see `results.png` which is a graph visualising simulation.